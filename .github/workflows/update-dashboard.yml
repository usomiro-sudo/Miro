name: Atualizar painel de tendências MIRO Intelligence

on:
  schedule:
    # Segundas e quintas, 09:00 UTC (~06:00 horário de Brasília)
    - cron: "0 9 * * 1,4"
  workflow_dispatch: {}

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Ler tendências na home dos concorrentes
        working-directory: scripts
        run: python monitor_tendencias.py

      - name: Gerar dashboard
        working-directory: scripts
        run: python build_dashboard.py

      - name: Commitar dados e dashboard atualizados
        run: |
          git config user.name "miro-intelligence-bot"
          git config user.email "actions@github.com"
          git add data/ dashboard/
          git diff --cached --quiet || git commit -m "chore: atualizar tendências ($(date -u +%Y-%m-%d))"
          git push

      - name: Publicar no GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dashboard

  deploy-pages:
    needs: update
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
